@page "/"
@inject NavigationManager NavigationManager

<PageTitle>Index</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/portfolio", replace: true);
    }
}